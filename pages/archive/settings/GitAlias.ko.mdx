import { Comment } from 'components'
import { BackTop } from 'containers'

<BackTop />

# Git Alias

```bash
git config --global alias.pod "push origin dev"
git config --global alias.acm '!git add . && git commit -m'
git config --global alias.l "log --branches --decorate --graph --relative-date --oneline"
```

<Comment />
