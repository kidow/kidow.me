import { Comment, Card, Required } from 'components'
import { BackTop } from 'containers'
import { CubeIcon } from '@heroicons/react/24/outline'

<BackTop />

# AutoComplete

## Example

## Steps

<div className="steps">

### Prerequisite

<Card.List>
  <Card.Component title="Input" href="/archive/inputs/Input" Icon={CubeIcon} />
</Card.List>

### Reset CSS

datalist 고유의 picker icon을 삭제해 줍니다.

```css filename="styles/global.css"
input::-webkit-calendar-picker-indicator {
  opacity: 0;
}
```

### Copy Code

```tsx filename="components/AutoComplete/index.tsx"
import { useId } from 'react'
import type { FC, OptionHTMLAttributes, ComponentProps } from 'react'
import { Input } from 'components'

interface Props extends ComponentProps<typeof Input> {
  options: Array<OptionHTMLAttributes<HTMLOptionElement>['value']>
  id?: string
}

const Autocomplete: FC<Props> = ({ options, ...props }) => {
  const id = useId()
  return (
    <>
      <Input {...props} list={props.id || id} />
      <datalist id={props.id || id}>
        {options.map((value, key) => (
          <option value={value} key={key} />
        ))}
      </datalist>
    </>
  )
}

export default Autocomplete
```

### Usage

</div>

### Props

| Name                    |      Type       | Default |
| :---------------------- | :-------------: | :-----: |
| **options**<Required /> | `Array<string>` |         |
| **id**                  |     string      |         |

### References

https://developer.mozilla.org/ko/docs/Web/HTML/Element/datalist

<Comment />
